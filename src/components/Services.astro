---
import { siteConfig } from "../config";
---

<section class="section-shell bg-slate-950 text-white">
  <div class="mx-auto max-w-7xl px-6 py-20 sm:px-10 sm:py-24 lg:px-12 lg:py-28">
    <div class="flex flex-col justify-between gap-6 sm:flex-row sm:items-end">
      <div>
        <p class="section-kicker text-teal-300">02 / What I do</p>
        <h2 class="mt-4 max-w-2xl text-3xl font-black tracking-[-0.04em] text-white sm:text-5xl">From first prototype to dependable operation.</h2>
      </div>
      <p class="max-w-sm text-sm leading-6 text-slate-400">A practical bridge between industrial constraints and software possibilities.</p>
    </div>

    <div class="mt-12 grid gap-4 md:grid-cols-3">
      {siteConfig.services.map((service, index) => (
        <article class="group rounded-2xl border border-white/10 bg-white/[0.06] p-6 transition hover:-translate-y-1 hover:border-teal-400/40 hover:bg-white/[0.1] sm:p-7">
          <div class="flex items-start justify-between gap-4">
            <span class="font-mono text-xs font-bold tracking-[0.2em] text-teal-300">0{index + 1}</span>
            <span class="flex h-10 w-10 items-center justify-center rounded-xl bg-teal-400/10 text-teal-300">
              {service.icon === "chip" ? (
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="6" y="6" width="12" height="12" rx="2" /><path d="M9 9h6v6H9zM9 2v4M15 2v4M9 18v4M15 18v4M2 9h4M2 15h4M18 9h4M18 15h4" /></svg>
              ) : service.icon === "gauge" ? (
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 16a8 8 0 1 1 16 0" /><path d="m12 12 4-4" /><path d="M5 20h14" /></svg>
              ) : (
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 18a8 8 0 0 1 16 0" /><path d="M12 18v-5" /><path d="M5 4v3M19 4v3M3 10h3M18 10h3" /></svg>
              )}
            </span>
          </div>
          <h3 class="mt-10 text-xl font-bold tracking-tight text-white">{service.title}</h3>
          <p class="mt-3 text-sm leading-6 text-slate-400">{service.description}</p>
        </article>
      ))}
    </div>
  </div>
</section>
